# dotfiles
Repo to store my configs

### Requirements
- `git` for cloning this repo

### Supported OS
- Debian-based OS using `apt`

### Installation order

1. `bash create-symlinks.sh`
2. `bash install-packages.sh`